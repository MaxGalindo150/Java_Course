public class MathematicOps {
    public static void main(String[] arg){
        double x = 2.1,
               y = 3;
        
               //ceil method: it returns the integer part of a "double" number upward
               System.out.println(Math.ceil(x));

               //floor method: it returns the unteger part of a "double" number downward
               System.out.println(Math.floor(x));

               //pow method: raises a number to some power
               System.out.println(Math.pow(x,y));

               //there are a lot of methods in the Math class


    }
    
}
